/*Add numbers*/
addNumbers = (a, b) => {
  return a + b;
};
var sum = addNumbers(5, 4);

/*Subtract numbers*/
subtractNumbers = (a, b) => {
  return a - b;
};
var subtract = subtractNumbers(6, 1);

/*Multiply numbers*/
multiplyNumbers = (a, b) => {
  return a * b;
};
let multiply = multiplyNumbers(10, 5);

/*Divide numbers*/
divideNumbers = (a, b) => {
  return a / b;
};
let divide = divideNumbers(20, 2);
